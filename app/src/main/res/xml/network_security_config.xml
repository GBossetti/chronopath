<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration for the app -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust only system CAs -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration: allows user-installed CAs for testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
